# CopilotX Reverse Proxy — Caddy auto-HTTPS
# Usage: sudo cp Caddyfile /etc/caddy/Caddyfile && sudo systemctl reload caddy
#
# Replace YOUR_DOMAIN with your actual domain (e.g., api.example.com)
# Caddy will automatically obtain and renew SSL certificates via Let's Encrypt.

YOUR_DOMAIN {
    reverse_proxy localhost:24680

    # Optional: rate limiting (requires caddy-ratelimit plugin)
    # rate_limit {remote.host} 60r/m

    log {
        output file /var/log/caddy/copilotx.log
        format json
    }
}
