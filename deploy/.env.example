# CopilotX Remote Deployment Environment
# Copy this file to ~/.copilotx/.env and fill in your values
#
# mkdir -p ~/.copilotx
# cp .env.example ~/.copilotx/.env
# Edit ~/.copilotx/.env with your API key

# Required: API key for remote access (generate a strong random string)
# Generate with: openssl rand -hex 32
# Or use a custom prefix: echo "polly-copilotx-$(openssl rand -hex 16)"
COPILOTX_API_KEY=your-secret-api-key-here
